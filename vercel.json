{
  "buildCommand": "pnpm install && pnpm --filter @workspace/zenith run build",
  "outputDirectory": "artifacts/zenith/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
